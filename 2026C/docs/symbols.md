本文件按项目约定汇总建模文档中的符号。

## 时间相关

| 符号                           | 含义                                                  | 单位或范围                               |
| ---------------------------- | --------------------------------------------------- | ----------------------------------- |
| $N$                          | 一天的调度时段数                                            | 144                                 |
| $\Delta t$                   | 单个调度时段长度                                            | $1/6$ h                             |
| $t$                          | 调度时段或状态边界索引                                         | 时段为 $1,\ldots,N$；状态边界为 $0,\ldots,N$ |
| $\tau_t$                     | 第 $t$ 个时间边界，自当天 0:00 起累计                            | h，$\tau_t=t\Delta t$                |
| $I_t$                        | 第 $t$ 个调度区间                                         | $[\tau_{t-1},\tau_t)$               |
| $p_t$                        | 第 $t$ 时段外网购电电价                                      | 元/kWh                               |
| $L_t$                        | 第 $t$ 时段小区负载功率                                      | kW                                  |
| $\widehat P_t^{\mathrm{pv}}$ | 第 $t$ 时段光伏预测功率；帽号表示预测值                              | kW                                  |
| $\ell_t$                     | 第 $t$ 时段负载电量，$L_t\Delta t$                          | kWh                                 |
| $v_t$                        | 第 $t$ 时段可用光伏电量，$\widehat P_t^{\mathrm{pv}}\Delta t$ | kWh                                 |

## 储能参数

| 符号                        | 含义                     | 单位或取值                             |
| ------------------------- | ---------------------- | --------------------------------- |
| $E_{\mathrm{rated}}$      | 储能额定最大容量               | 12000 kWh                         |
| $E_{\min}$                | 运行储电量下限                | 1200 kWh                          |
| $E_{\max}$                | 运行储电量上限                | 10800 kWh                         |
| $E_{\mathrm{init}}$       | 当天 0:00 的已知内部储电量       | kWh；问题一按题给初值及逐日日末回归条件取 6000       |
| $P_{\max}^{\mathrm{ch}}$  | 母线侧最大充电功率              | 5000 kW                           |
| $P_{\max}^{\mathrm{dis}}$ | 母线侧最大放电功率              | 5000 kW                           |
| $\eta_{\mathrm{ch}}$      | 充电效率                   | 无量纲，0.9                           |
| $\eta_{\mathrm{dis}}$     | 放电效率                   | 无量纲，0.9                           |

## 通用的决策变量与状态

| 符号    | 含义                       | 单位或范围                             |
| ----- | ------------------------ | --------------------------------- |
| $g_t$ | 第 $t$ 时段外网计划购电量          | kWh，非负                            |
| $c_t$ | 第 $t$ 时段储能设备的充电量         | kWh，非负                            |
| $d_t$ | 第 $t$ 时段储能设备的放电量         | kWh，非负                            |
| $r_t$ | 第 $t$ 时段未利用的可用光伏电量       | kWh，$0\le r_t\le v_t$             |
| $e_t$ | 时间边界 $\tau_t$ 的电池内部储电量   | kWh，$E_{\min}\le e_t\le E_{\max}$ |
| $u_t$ | 储能运行模式变量，$u_t\in\{0,1\}$ | $u_t=1$ 时允许充电，$u_t=0$ 时允许放电。      |

## 问题一的输出

| 符号                  | 含义                                       | 单位或范围        |
| ------------------- | ---------------------------------------- | ------------ |
| $J$                 | 全天购电费用，模型最小化目标                           | 元            |
| $W_{\mathrm{loss}}$ | 全天储能充、放电造成的电量损耗                          | kWh          |
| $G_{\mathrm{day}}$  | 最优方案的全天外网购电量，$\sum_{t=1}^{N}g_t^{\star}$ | kWh          |

## 问题二新增变量与输出

### 日期、数据与历史信息

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $d$ | 正式评价日期索引；下标中的 $d$ 表示日期，$d_{d,t}$ 整体表示放电量 | $d=1,\ldots,334$，依次对应 2025 年 2 月 1 日至 12 月 31 日 |
| $j$ | 历史日期索引，按真实日期先后比较；初始化历史也包含 1 月份 | 早于计划日期 $d$ |
| $D_d,D_j$ | 日期 $d$、$j$ 对应的日序数，二者之差表示相隔天数 | 天，采用同一日历起点 |
| $\mathcal H_d$ | 制定第 $d$ 天计划时已经取得实际数据的历史日期集合 | $\{j:D_j<D_d\}$，范围为附件 2 中已结束的日期；包含已完成的 1 月初始化数据，1 月 1 日为空 |
| $\mathcal F_{d-1}$ | 制定第 $d$ 天 0:00 计划时可用的信息集，包含历史实测、附件 1 先验、已知电价与日历、已结束日期的验证记录和实际初始储电量 | 不含第 $d$ 天及以后日期的实际数据；预测、参数与全部计划变量均由此确定 |
| $\ell_t^{\mathrm{prior}},v_t^{\mathrm{prior}}$ | 附件 1 的冷启动负载电量、光伏电量，在初始化开始前已知；光伏按固定近零阈值清洗 | kWh，非负；无可用历史样本时用于预测，不计为历史实测 |
| $a(j),a(d)$ | 日期所属的负载类型 | 高负载日：周日至周四；低负载日：周五、周六 |
| $L_{j,t}$ | 历史日期 $j$ 第 $t$ 时段的实际小区负载功率 | kW，非负 |
| $P_{j,t}^{\mathrm{pv}}$ | 历史日期 $j$ 第 $t$ 时段的原始实际光伏功率 | kW，非负 |
| $\widetilde P_{j,t}^{\mathrm{pv}}$ | 对近零噪声归零后的实际光伏功率；波浪号表示清洗值 | kW，非负 |
| $\varepsilon_{\mathrm{pv}}$ | 光伏近零噪声归零阈值 | 1 kW；仅将低于该阈值的非负功率置零 |
| $\ell_{d,t},\ell_{j,t}$ | 当天或历史日期第 $t$ 时段的实际小区负载电量 | kWh，非负，负载功率乘以 $\Delta t$ |
| $v_{d,t},v_{j,t}$ | 当天或历史日期第 $t$ 时段清洗后的实际可用光伏电量 | kWh，非负，清洗后光伏功率乘以 $\Delta t$ |
| $n_{d,t}$ | 实际净负荷，$\ell_{d,t}-v_{d,t}$ | kWh，可为负 |

### 滚动预测与分位数

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $\rho_L,\rho_V$ | 当前计划日选定的负载、光伏历史样本指数衰减时间尺度；省略日期下标，可随历史验证逐日更新 | 天，正数；无可用验证记录时均预设为 14，验证日期必须早于计划日 |
| $\omega_{d,j}^{L},\omega_{d,j}^{V}$ | 日期 $j$ 的历史样本在预测日期 $d$ 时的负载权重、光伏权重 | 无量纲，非负；负载只使用同类日期 |
| $Q_q^{(\omega)}$ | 权重为 $\omega$ 的加权 $q$ 分位数算子 | 输出单位与样本一致；总权重须大于 0 |
| $q$ | 加权分位数和分位数损失中的分位数水平 | 无量纲，$0<q<1$；负载取 0.8，光伏取 0.2 |
| $m,i$ | 加权分位数定义中的样本数和样本索引 | $m$ 为正整数，$i=1,\ldots,m$ |
| $x_i,\omega_i$ | 加权分位数定义中的第 $i$ 个样本及其权重 | $x_i$ 与被预测量同单位；$\omega_i\ge0$ |
| $z$ | 加权分位数定义中搜索的候选阈值，与日末偏差变量 $z_d^\pm$ 不同 | 与样本同单位 |
| $\widehat\ell_{d,t}$ | 第 $d$ 天 0:00 生成并保存的风险修正负载预测；无同类历史时采用先验 | kWh，非负；$\widehat\ell_{\mathrm{2025.1.1},t}=\ell_t^{\mathrm{prior}}$ |
| $\widehat v_{d,t}$ | 第 $d$ 天 0:00 生成并保存的风险修正光伏预测；无历史时采用先验 | kWh，非负；$\widehat v_{\mathrm{2025.1.1},t}=v_t^{\mathrm{prior}}$ |
| $\mathbf 1\{\cdot\},\mathbf 1(\cdot)$ | 指示函数，条件成立取 1，否则取 0 | 无量纲 |
| $[x]^+,(x)^+$ | 正部函数，均表示 $\max(x,0)$ | 与 $x$ 同单位 |

### 计划变量与实际状态

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $g_{d,t}^{\mathrm{plan}}$ | 第 $d$ 天在 0:00 确定的第 $t$ 时段计划购电量 | kWh，非负 |
| $\bar c_{d,t},\bar d_{d,t}$ | 0:00 下达并冻结的计划充电量、计划放电量；横线表示计划值，实际执行仅按预定规则向下截断 | kWh，非负，受设备功率及模式限制 |
| $\bar r_{d,t}$ | 预测情景下的计划弃光量 | kWh，$0\le\bar r_{d,t}\le\widehat v_{d,t}$ |
| $\bar e_{d,t}$ | 计划轨迹中第 $t$ 个时间边界的储电量 | kWh，$E_{\min}\le\bar e_{d,t}\le E_{\max}$ |
| $u_{d,t}$ | 第 $d$ 天 0:00 确定并冻结的第 $t$ 时段储能运行模式 | $\{0,1\}$；1 允许充电，0 允许放电，两者均允许空闲 |
| $c_{d,t},d_{d,t}$ | 实际执行的充电量、放电量，均以母线侧电量计 | kWh，$0\le c_{d,t}\le\bar c_{d,t}$，$0\le d_{d,t}\le\bar d_{d,t}$ |
| $e_{d,t}$ | 第 $d$ 天第 $t$ 个时间边界的实际内部储电量 | kWh，$E_{\min}\le e_{d,t}\le E_{\max}$ |
| $e_{\mathrm{2025.1.1},0}$ | 2025 年 1 月 1 日 0:00 的初始储电量 | 6000 kWh |
| $e_{\mathrm{2025.1.31},N}$ | 1 月 31 日初始化回放结束后的实际储电量，也是 $e_{1,0}$ | kWh，由初始化模拟给出，不以计划日末状态代替 |
| $E_{\mathrm{end}}^{\min}$ | 问题二年末储电量下限，对应脚本 `E_TERMINAL_MIN` | 5400 kWh |
| $E_{\mathrm{end}}^{\max}$ | 问题二年末储电量上限，对应脚本 `E_TERMINAL_MAX` | 6600 kWh |
| $\bar e_{334,N},e_{334,N}$ | 问题二 12 月 31 日的计划、实际日末储电量，分别进行终端区间校验；计划达标不能代替实际达标 | kWh，均须位于 $[E_{\mathrm{end}}^{\min},E_{\mathrm{end}}^{\max}]$ |
| $h_{d,t}$ | 仅用于弥补当期实际负载缺口的紧急购电量 | kWh，非负；按 $5p_t$ 计费 |
| $s_{d,t}$ | 满足实际负载和实际充电后的剩余电量 | kWh，非负，$s_{d,t}=r_{d,t}+w_{d,t}$ |
| $r_{d,t}$ | 实际弃光量，按先弃光的口径从剩余电量中分解 | kWh，$0\le r_{d,t}\le v_{d,t}$ |
| $w_{d,t}$ | 实际未利用但仍需付费的计划购电量 | kWh，$0\le w_{d,t}\le g_{d,t}^{\mathrm{plan}}$ |
| $E_{\mathrm{ref}}$ | 日末储电量软惩罚所用的参考值，不等于每天强制回归的初值 | 6000 kWh |
| $z_d^+,z_d^-$ | 计划日末储电量相对参考值的正、负偏差辅助变量，满足 $\bar e_{d,N}-E_{\mathrm{ref}}=z_d^+-z_d^-$ | kWh，非负 |
| $\boldsymbol{x}_d$ | 第 $d$ 天日计划 MILP 的全部决策变量组成的列向量 | $6N+3=867$ 维，分量按各变量自身单位解释 |

### 费用、评价与辅助推导

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $J^{\mathrm{real}}$ | 2 月 1 日至 12 月 31 日的实际总购电费用，包含计划购电费和紧急购电费 | 元 |
| $J_{\mathrm{Jan}}^{\mathrm{real}}$ | 1 月 1 日至 1 月 31 日初始化阶段的实际计划购电费与紧急购电费之和 | 元，不含储电量软惩罚 |
| $J_{365}^{\mathrm{real}}$ | 1 月 1 日至 12 月 31 日完整 365 天的实际总购电费用，在模型校验 CSV 中汇总 | 元，$J_{\mathrm{Jan}}^{\mathrm{real}}+J^{\mathrm{real}}$，不含储电量软惩罚 |
| $\widehat J_d$ | 第 $d$ 天计划模型的优化目标，包含计划购电费和日末储电量偏差惩罚 | 元，不等同于实际结算费用 |
| $\lambda_E$ | 当前计划日的日末储电量偏差惩罚系数；省略日期下标，由严格早于计划日的验证记录选择 | 元/kWh，非负；无可用验证记录时预设为 $\max_t p_t$；验证评分另用统一系数 $\max_t p_t/\eta_{\mathrm{ch}}$ |
| $X$ | 解释最优分位数时使用的单时段随机净需求 | kWh |
| $q$（单时段费用推导中） | 计划覆盖的电量；该处沿用 Q2 原文写法，与分位数水平 $q$ 按上下文区分 | kWh，非负 |
| $p$ | 单时段费用推导中的正常电价，即不带时段索引的电价 | 元/kWh |
| $C(q,X)$ | 单时段费用，$pq+5p(X-q)^+$ | 元 |
| $\mathbb E[\cdot]$ | 对随机量取期望的算子 | 与被取期望的量同单位 |
| $\Pr(\cdot)$ | 事件发生的概率 | 无量纲，$[0,1]$ |
| $F_X$ | 随机净需求 $X$ 的累积分布函数，$F_X(q)=\Pr(X\le q)$ | 无量纲，$[0,1]$ |
| $y,\widehat y$ | 预测误差评价中的实际值和预测值 | 两者必须使用同一单位 |
| $L_q(y,\widehat y)$ | 分位数损失，$(q-\mathbf 1\{y<\widehat y\})(y-\widehat y)$ | 与 $y$ 同单位 |
| MAE、RMSE | 平均绝对误差、均方根误差 | 与被评价的预测量同单位 |
| SOC | 荷电状态，即内部储电量与额定容量之比 | 无量纲，$e_{d,t}/E_{\mathrm{rated}}$，也可用百分比表示 |

## 问题三新增变量与输出

Q3 沿用 Q2 的日期、实际负载、实际光伏和储能物理符号。括号上标 $(k)$ 表示第 $k$ 次决策时形成的版本，不表示乘方；横线表示预测优化中的候选量或计划值。候选变量采用 $(d,s,k)$ 索引：$d$ 是决策日期，$s$ 是以该日0点为原点、允许延伸到次日的展开时段索引。实际变量和正式承诺仍用日期 $d$ 与日内时段 $t$；读取当天候选量时取 $s=t$。$\bar g_{d,s}^{(k)}$ 是候选购电量，$g_{d,t}^{(k)}$ 是正式承诺；0点全天提交和日内当前窗口提交时二者对应相等，次日候选不提前形成承诺。Q3 的 $w_{d,t}$ 表示最终有效购电量中未利用的部分，其上界为 $g_{d,t}^{\mathrm{eff}}$。

### 决策时刻、预报与信息

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $k$ | 日内预报发布与决策机会的编号 | $0,1,2,3$，分别对应 0:00、6:00、12:00、18:00 |
| $\theta_k$ | 第 $k$ 次决策时刻，自当天 0:00 起累计 | h，$\theta_k=6k$ |
| $b_k$ | 第 $k$ 次决策时已经完成的 10 分钟时段数，也是对应状态边界索引 | $b_k=36k$，取 0、36、72、108 |
| $s$ | 预测优化使用的展开时段索引，以决策日0点为原点；也用于该时段结束后的预测状态边界 | 候选时段为 $b_k+1,\ldots,M_{d,k}$，状态边界另含 $b_k$；最大展开时段为 252 |
| $\delta(s)$ | 目标供电时段相对于决策日期的天数偏移 | $\lfloor(s-1)/N\rfloor$，取 0 或 1；仅用于时段映射 |
| $t(s)$ | 展开时段 $s$ 对应的目标日内时段 | $s-N\delta(s)\in\{1,\ldots,N\}$；目标日期为 $d+\delta(s)$，电价为 $p_{t(s)}$ |
| $M_{d,k}$ | 本次优化最后一个展开时段及最终状态边界的索引 | 普通日期为 $b_k+N$；12月31日为 $N$，在评价终点截断 |
| $\mathcal W_{d,k}$ | 本次完整优化窗口，主方案固定向前24小时，12月31日除外 | $\{b_k+1,\ldots,M_{d,k}\}$；普通日期均含 144 个时段 |
| $\mathcal T_k$ | 优化窗口中属于当天的剩余时段集合，用于日内优化的当天候选增减购成本 | $\{b_k+1,\ldots,N\}$，依次含 144、108、72、36 个时段 |
| $\mathcal U_{d,k}$ | 优化窗口中的次日前瞻时段集合，只估计未来初始购电成本 | $\{N+1,\ldots,M_{d,k}\}$；普通日期依次含 0、36、72、108 个时段，12月31日为空 |
| $\mathcal B_k$ | 第 $k$ 次决策后实际执行的当天 6 小时窗口；$k=1,2,3$ 时也是唯一允许正式调整购电量的窗口 | $\{b_k+1,\ldots,b_k+36\}$，对应 $[6k,6(k+1))$ h；0点购电提交范围仍为全天 |
| $\nu$ | 附件 3 每次预报的整点提前量索引 | $1,\ldots,24$；表示发布后 $\nu$ 小时 |
| $\widehat P_{d,k,\nu}^{\mathrm{pv}}$ | 第 $d$ 天第 $k$ 次发布、对应发布后第 $\nu$ 个整点的光伏功率预报 | kW，非负 |
| $\widehat v_{d,s}^{(k)}$ | 第 $k$ 次决策对展开时段 $s$ 使用的光伏预测电量，覆盖当前及次日目标时段 | kWh，非负；主方案为 $\widehat P_{d,k,\lceil(s-b_k)/6\rceil}^{\mathrm{pv}}\Delta t$，必要时仅作历史误差修正 |
| $\widehat\ell_{d,s}^{(k)}$ | 第 $k$ 次决策对目标日期 $d+\delta(s)$、目标时段 $t(s)$ 生成的负载预测电量 | kWh，非负；按目标日期的负载类型及当前可用历史预测 |
| $\mathcal F_{d,k}$ | 第 $d$ 天第 $k$ 次决策时可用的信息集 | 历史数据、当日已完成时段实测、实际储电量、已知电价及截至此时已发布的预报 |
| $k(t)$ | 第 $t$ 个时段执行前最近的一次决策编号 | $\lfloor(t-1)/36\rfloor$，取 $0,1,2,3$ |
| $\lceil\cdot\rceil,\lfloor\cdot\rfloor$ | 向上取整、向下取整算子，用于预报提前量及执行阶段的时间映射 | 输出整数 |

### 购电调整与储能计划

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $\bar g_{d,s}^{(k)}$ | 第 $k$ 次优化形成的非紧急候选购电量，跨日部分仅用于前瞻计算 | kWh，非负，$s\in\mathcal W_{d,k}$；0点提交全天，日内仅提交 $\mathcal B_k$ 内的量，次日重新制定初始计划 |
| $g_{d,t}^{(k)}$ | 第 $k$ 次决策后，第 $t$ 时段的正式购电承诺量 | kWh，非负；$g_{d,t}^{(0)}=g_{d,t}^{\mathrm{plan}}$，日内仅在 $\mathcal B_k$ 内更新，窗口外沿用上一版正式承诺 |
| $\Delta g_{d,k,t}^{+}$ | 第 $k$ 个日内调整窗口正式提交的、相对于0点初始计划的增购量 | kWh，$[g_{d,t}^{(k)}-g_{d,t}^{\mathrm{plan}}]^+$；$k=1,2,3$，$t\in\mathcal B_k$，窗口外为 0 |
| $\Delta g_{d,k,t}^{-}$ | 第 $k$ 个日内调整窗口正式提交的、相对于0点初始计划的减购量 | kWh，$[g_{d,t}^{\mathrm{plan}}-g_{d,t}^{(k)}]^+$；$t\in\mathcal B_k$，不超过初始计划量，窗口外为 0 |
| $g_{d,t}^{\mathrm{eff}}$ | 按四个执行窗口拼接得到的最终有效非紧急购电量 | kWh，$g_{d,t}^{\mathrm{eff}}=g_{d,t}^{(k(t))}$；0-6点及未调整时段等于初始计划量 |
| $\bar c_{d,s}^{(k)},\bar d_{d,s}^{(k)}$ | 第 $k$ 次决策在完整优化窗口内规划的充电量、放电量，仅当前6小时用于实际执行 | kWh，非负，$s\in\mathcal W_{d,k}$，受设备功率及模式限制；当天执行公式取 $s=t$ |
| $\bar h_{d,s}^{(k)}$ | 预测情景下补足负载缺口的备用购电估计，不是已经发生的紧急购电 | kWh，$0\le\bar h_{d,s}^{(k)}\le\widehat\ell_{d,s}^{(k)}(1-u_{d,s}^{(k)})$；预测成本为 $5p_{t(s)}\bar h_{d,s}^{(k)}$ |
| $\bar r_{d,s}^{(k)}$ | 第 $k$ 次决策预测情景下的计划弃光量 | kWh，$0\le\bar r_{d,s}^{(k)}\le\widehat v_{d,s}^{(k)}$ |
| $\bar w_{d,s}^{(k)}$ | 第 $k$ 次决策预测情景下未利用的候选购电量 | kWh，$0\le\bar w_{d,s}^{(k)}\le\bar g_{d,s}^{(k)}$ |
| $\bar e_{d,s}^{(k)}$ | 本次覆盖完整优化窗口的计划储电量轨迹，跨午夜连续，远期部分仅用于前瞻 | kWh，$E_{\min}\le\bar e_{d,s}^{(k)}\le E_{\max}$，初值为实际 $e_{d,b_k}$；最终状态为 $\bar e_{d,M_{d,k}}^{(k)}$ |
| $u_{d,s}^{(k)}$ | 第 $k$ 次决策在展开时段 $s$ 安排的储能运行模式，仅当前窗口用于实际执行 | $\{0,1\}$；1 允许充电且禁止备用购电估计，0 允许放电 |

### 费用与结果

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $\widehat J_{3,d}^{(k)}$ | 第 $k$ 次滚动优化最小化的预测目标；0点计初始购电，日内计当天候选调整和次日候选初始购电，另计预测备用购电及末端软惩罚 | 元；不等于实际结算费用，也不跨轮累计 |
| $E$（$\Phi_{d,k}$ 的自变量） | 传入窗口末端惩罚函数的电池内部储电量 | kWh，优化时取 $\bar e_{d,M_{d,k}}^{(k)}$ |
| $E_{\mathrm{ref}}$（Q3） | 普通日期的滚动窗口末端参考储电量，沿用 Q2 的数值，不是每天或每6小时的硬目标 | 6000 kWh |
| $\lambda_{E,d,k}^{(3)}$ | Q3 第 $k$ 次决策使用的窗口末端储电量软惩罚系数 | 元/kWh，非负；无验证记录时为 $\max_t p_t$，之后只由当前已完成的历史验证选择 |
| $\Phi_{d,k}(E)$ | 对优化窗口结束后储能价值的软惩罚近似，仅用于预测目标 | 元；普通日期为 $\lambda_{E,d,k}^{(3)}\lvert E-E_{\mathrm{ref}}\rvert$，12月31日为 0 并改用硬终端约束 |
| $J_{3,d}^{\mathrm{real}}$ | Q3 第 $d$ 天实际总购电费用，包含初始计划电费、三个互不重叠窗口的正式增减购费用和实际紧急购电费 | 元；初始电费不退，增购按 $1.5p_t$、减购违约按 $0.5p_t$、紧急购电按 $5p_t$ 计费 |
| $J_3^{\mathrm{real}}$ | Q3 在 2 月 1 日至 12 月 31 日的实际总购电费用 | 元，$\sum_{d=1}^{334}J_{3,d}^{\mathrm{real}}$ |

上述调整费用中的 $p_t$ 均为被调整电量对应的供电时段电价，不是提交调整指令的时刻电价。每个供电时段最多正式调整一次，增减购均相对于0点初始计划计算；0-6点不作日内调整。初始计划、各窗口正式调整和实际紧急购电分别记账，有效购电量不再重复收取普通电费。

预测目标按目标日期划分：当天候选购电相对已存在的初始计划计算增减购成本，次日候选购电按正常电价 $p_{t(s)}$ 估计未来初始购电成本。只有当前提交窗口的正式调整在本次结算；次日候选、预测备用购电和窗口末端软惩罚均不直接计入实际费用。次日初始计划由次日0点重新制定并记账，不能累计前一日各轮候选费用。
